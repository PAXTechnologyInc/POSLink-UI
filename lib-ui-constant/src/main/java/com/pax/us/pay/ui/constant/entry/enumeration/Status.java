package com.pax.us.pay.ui.constant.entry.enumeration;

public class Status {
    public enum State {STARTED, COMPLETED, ONGOING, REQUIRED}
    public enum Type {SUCCESS, FAIL, PROCESSING, INSTRUCTION}
}
